{
  "name": "chigasaki-gomi-skill",
  "version": "1.0.0",
  "private": true,
  "description": "茅ヶ崎市のゴミ収集日を教えてくれる個人利用Alexaスキル",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "validate-calendar": "tsx scripts/validateCalendar.ts",
    "deploy": "npm run build && node scripts/deploy.mjs"
  },
  "dependencies": {
    "ask-sdk-core": "^2.14.0",
    "ask-sdk-model": "^1.85.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.5",
    "tsx": "^4.16.0",
    "typescript": "^5.5.3",
    "vitest": "^1.6.0"
  }
}
